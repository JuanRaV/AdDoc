extends ../layout/index
block contenido
    .py-10 
        h1.text-4xl.my-10.text-center.font-extrabold Ad
            span.text-blue-600 Doc
        h2.text-center.text-2xl.font-extrabold.mb-3= pagina

        div(class="bg-white shadow py-8 px-4 rounded max-w-md mx-auto")
            p(class= error?'bg-red-500':'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= message
        
        if !error
            a(href="/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") You can start session now